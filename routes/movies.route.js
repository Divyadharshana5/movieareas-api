import express from "express";
const router = express.Router();

//R-For Read
app.get("/movies", () => {});

//C-For Create
app.post("/movies", () => {});

//U-For Update
app.put("/movies/:id", () => {});

//D-For Delete
app.delete("/movies/:id", () => {});
